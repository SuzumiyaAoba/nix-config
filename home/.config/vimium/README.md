Vimium settings are managed in `vimium-options.json`.

Apply them from Vimium's Options page:

1. Open Vimium Options.
2. Go to the backup section.
3. Choose "Restore from backup".
4. Select `~/.config/vimium/vimium-options.json`.
